import 'package:flutter/material.dart';

class MyTracks extends StatelessWidget {
  const MyTracks({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Dummy My Tracks page"),
    );
  }
}
